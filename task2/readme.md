main.yml -> This is the mail file for deployment.
ansiblekey.pem -> This the private key (Create your own key on AWS).
vault.yml -> This is the file which gets encrypted and is used to store all critical data such as username and passwords.
